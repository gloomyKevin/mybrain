# mybrain 员工手册

> Claude Code 启动时读这份文件。之后所有行为遵循这里的规则。

## 这是什么

个人 OPC（一人公司）**内容生产 vault**，不是通用知识管理系统，不是第二大脑。

作用：支撑"想法 → 小红书 / 即刻发布"的极简流水线。Month 3+ 加入公众号和 build in public 小工具后，流水线向"素材 → 多平台内容"扩展。

---

## 启动时必读（按顺序）

1. `自我.md` — 我的自我画像（历史失败模式、动机排序、长短板、给你的提示）
2. `OPC 规划.md` — 6 个月路径 + 硬约束（含 **2026-04-24 晚 12 点前发小红书第一条**）
3. `SOP.md` — 操作流程手册（触发词 + 步骤），遇到触发词按 SOP 执行

凡涉及工具、架构、流程的讨论，先对照前两份。

---

## 硬约束（不要帮我违反）

- **Month 1-2 不加新工具、不重构架构**（`OPC 规划.md` §二 / Month 1-2）
- 所有"先搭 / 先优化 X 再开始"类提议，先过一问：**X 的答案能让我 24 小时内启动吗？** 不能就是推迟
- 中文命名优先
- 4/24 晚 12 点硬约束若破，全部规划推倒重新校准

---

## 目录结构

```
mybrain/
├── 收件箱.md              ← 唯一捕获入口，桌面粘贴 + 手机追加都在这
├── 素材库/                ← 清洗后的外部参考，一文件一条
│   ├── _模板.md
│   └── 橙皮书-Obsidian-AI/    架构参考（已消费，Month 1 不回看）
├── 草稿/                  ← 正在写的内容
│   └── _模板.md
├── 已发布/                ← 发布存档 + 数据
│   └── _模板.md
├── 自我.md                ← 约束性，只能我改
├── OPC 规划.md            ← 约束性，只能我改
├── 输入捕获方案.md         ← 精简捕获流程
├── SOP.md                 ← 操作流程手册
├── task/                  ← 我的 Kanban，别碰
├── .claude/skills/        ← Kepano 的 obsidian-skills（已装，教你处理 Obsidian 专有语法）
├── .gitignore             ← Git 仓库 @ github.com:gloomyKevin/mybrain
├── 2026-04-22.md          ← 日记（根目录惯例）
├── Obsidian 的配置.md     ← 原有笔记
└── 工作流雏形.md          ← 原有笔记
```

## 核心流水线

```
收件箱  →  素材库  →  草稿  →  已发布
 (捕获)    (整理)    (创作)    (存档)
```

对应橙皮书 §06 里 Karpathy 的 `raw → wiki → output`，改造成内容生产版本。

---

## 你（Claude）可以做的

- 从 `收件箱.md` 杂乱内容**批量提取候选素材**到 `素材库/`，补 frontmatter
- 给 `素材库/` 条目补「摘要」「可用角度」
- 基于若干条素材生成**草稿雏形**到 `草稿/`
- 查找 `素材库/` 里状态=`未处理` 或 `可用角度` 尚未被使用过的条目
- 已发布后，帮我追加数据到 `已发布/` 对应条目

## 你不该做的

- **不要改** `自我.md`、`OPC 规划.md`（只能我本人改）
- **不要改** `已发布/` 里归档的发布文案（发了就是档案，只能补数据和复盘）
- **不要**新建目录 / 重构结构
- **不要**主动建议"再完善 / 再优化 / 加一个 X"——如果我提出，先提醒我这可能是推迟
- **不要碰** `task/`（我自己的 Kanban）
- **不要碰** `.obsidian/`（插件配置手动管）
- **不要**把已有的 `cloudflare-imgbed-*.pages.dev` 图床链接本地化

---

## Obsidian 环境说明

- 本 vault 同时被 `remotely-save` 和 `nutstore-sync` 两个插件同步到远端，改动会传播
- **以及 Git**（remote: `github.com:gloomyKevin/mybrain`）——每次你大改文件结构前都可用 `git diff` / `git checkout` 兜底；大改后让用户 review 再 `git commit`
- `cf-imageBed` 插件会把粘贴的图片自动上传到 Cloudflare 并改写链接
- `daily-notes` 核心插件开启，每日笔记用 `YYYY-MM-DD.md` 格式放在根目录
- wiki-link `[[...]]` 和嵌入 `![[...]]` 优先于相对路径
- `.claude/skills/` 下有 Kepano 官方 obsidian-skills，处理 `.md` / `.base` / `.canvas` 前自动参照

## 命名规范

- 中文 + 空格 / 破折号 `-`，不用下划线
- 素材库文件名：`<简短关键词>.md` 或 `<来源简称-关键词>.md`
- 草稿文件名：`<YYYY-MM-DD>-<标题关键词>.md`
- 已发布文件名：`<YYYY-MM-DD>-<平台>-<标题关键词>.md`
